# Verification

Run `python3 -m unittest discover -s tests` and `python3 tools/build-manifest.py`. Verify that the initial manifest has a nonempty version and an empty themes array. After adding approved content, verify hashes, byte counts, repository-scoped URLs, and the free, read-only Affiliate flow in Clink.
